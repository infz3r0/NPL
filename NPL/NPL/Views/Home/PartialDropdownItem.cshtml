@using NPL.Models;
@model IEnumerable<Loai>

@foreach (Loai item in Model)
{
    <a class="dropdown-item" href="@Url.Action("MonAnTheoLoai", "Home", new { idLoai = item.IDLoai })">@item.TenLoai</a>
}
